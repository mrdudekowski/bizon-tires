# 🎠 ИСПРАВЛЕНИЕ АДАПТИВНОЙ КАРУСЕЛИ

## ❌ **ПРОБЛЕМА:**

### **Старая реализация (НЕ РАБОТАЛА):**

```css
.carousel-track {
  display: flex;
  gap: 2rem;  /* ❌ Gap 2rem */
}

.carousel-slide {
  flex: 0 0 100%;  /* Мобильный: 1 карточка */
}

@media (min-width: 768px) {
  .carousel-slide {
    flex: 0 0 calc(50% - 1rem);  /* ❌ НЕВЕРНАЯ МАТЕМАТИКА */
  }
}

@media (min-width: 1024px) {
  .carousel-slide {
    flex: 0 0 calc(25% - 1.5rem);  /* ❌ НЕВЕРНАЯ МАТЕМАТИКА */
  }
}
```

### **Почему не работало:**

1. **Неправильный расчет с `gap`:**
   - `calc(50% - 1rem)` НЕ учитывает, что `gap: 2rem` распределяется МЕЖДУ слайдами
   - Для 2 карточек с `gap: 2rem`: нужно `calc((100% - 2rem) / 2)`, а не `calc(50% - 1rem)`
   - Для 4 карточек с `gap: 2rem`: нужно `calc((100% - 6rem) / 4)`, а не `calc(25% - 1.5rem)`

2. **Отсутствие промежуточных breakpoints:**
   - Не было 3 карточек на средних экранах
   - Резкий переход от 2 к 4 карточкам

3. **Слишком большой `gap`:**
   - `2rem` (32px) слишком много для мобильных устройств

---

## ✅ **РЕШЕНИЕ:**

### **Новая реализация (РАБОТАЕТ):**

```css
.carousel-track {
  display: flex;
  gap: 1.5rem;  /* ✅ Уменьшен gap до 1.5rem (24px) */
}

.carousel-slide {
  flex: 0 0 100%;  /* Мобильный: 1 карточка */
  min-width: 0;
  display: flex;
  justify-content: center;
}

/* Tablet: 2 cards */
@media (min-width: 640px) {
  .carousel-slide {
    flex: 0 0 calc((100% - 1.5rem) / 2);  /* ✅ ПРАВИЛЬНАЯ МАТЕМАТИКА */
  }
}

/* Desktop Small: 3 cards */
@media (min-width: 1024px) {
  .carousel-slide {
    flex: 0 0 calc((100% - 3rem) / 3);  /* ✅ ПРАВИЛЬНАЯ МАТЕМАТИКА */
  }
}

/* Desktop Large: 4 cards */
@media (min-width: 1280px) {
  .carousel-slide {
    flex: 0 0 calc((100% - 4.5rem) / 4);  /* ✅ ПРАВИЛЬНАЯ МАТЕМАТИКА */
  }
}
```

---

## 📐 **МАТЕМАТИКА:**

### **Формула для расчета ширины слайда:**

```
flex-basis = (100% - (gap × (количество_карточек - 1))) / количество_карточек
```

### **Примеры:**

| Карточек | Gap | Формула | Результат |
|----------|-----|---------|-----------|
| 1 | 1.5rem | `100%` | 100% |
| 2 | 1.5rem | `(100% - 1.5rem) / 2` | ~49.25% каждая |
| 3 | 1.5rem | `(100% - 3rem) / 3` | ~32.5% каждая |
| 4 | 1.5rem | `(100% - 4.5rem) / 4` | ~24.125% каждая |

---

## 🎯 **BREAKPOINTS:**

| Разрешение | Ширина экрана | Карточек | Описание |
|------------|---------------|----------|----------|
| **Mobile** | < 640px | 1 | Телефоны |
| **Tablet** | 640px - 1023px | 2 | Планшеты, маленькие ноутбуки |
| **Desktop Small** | 1024px - 1279px | 3 | Средние мониторы |
| **Desktop Large** | ≥ 1280px | 4 | Большие мониторы |

---

## 🔍 **ПОЧЕМУ ЭТО РАБОТАЕТ:**

### **1. Правильный расчет с `gap`:**

```
Для 2 карточек с gap: 1.5rem (24px):
┌─────────────────────────────────────┐
│  Card 1   │ 1.5rem gap │   Card 2   │
│  49.25%   │            │   49.25%   │
└─────────────────────────────────────┘
     ↑                          ↑
Ширина = (100% - 1.5rem) / 2
```

### **2. Плавное масштабирование:**

- **< 640px:** 1 карточка (мобильные)
- **640px:** 2 карточки (планшеты)
- **1024px:** 3 карточки (средние экраны)
- **1280px:** 4 карточки (большие экраны)

### **3. Использование best practices:**

Согласно документации Embla Carousel:
- ✅ Используем CSS `flex-basis` для адаптивности
- ✅ Embla автоматически пересчитывает при resize
- ✅ Не нужно использовать `breakpoints` в JS-конфиге
- ✅ `min-width: 0` предотвращает overflow

---

## 📊 **СРАВНЕНИЕ:**

### **Старая реализация:**

```
Mobile (< 768px):    1 карточка ✅
Tablet (768-1023px): 2 карточки (НО ЛОМАЛОСЬ из-за calc) ❌
Desktop (≥ 1024px):  4 карточки (НО ЛОМАЛОСЬ из-за calc) ❌
```

### **Новая реализация:**

```
Mobile (< 640px):      1 карточка ✅
Tablet (640-1023px):   2 карточки ✅
Desktop S (1024-1279): 3 карточки ✅
Desktop L (≥ 1280px):  4 карточки ✅
```

---

## 🧪 **ТЕСТИРОВАНИЕ:**

### **Как проверить:**

1. Откройте DevTools (F12)
2. Включите Device Toolbar (Ctrl+Shift+M)
3. Протестируйте на разных разрешениях:

| Устройство | Ширина | Ожидаемо карточек |
|------------|--------|-------------------|
| iPhone SE | 375px | 1 |
| iPhone 12 Pro | 390px | 1 |
| iPad Mini | 768px | 2 |
| iPad Pro | 1024px | 3 |
| MacBook Pro 13" | 1280px | 4 |
| Desktop 1920px | 1920px | 4 |

---

## 🎨 **ВИЗУАЛИЗАЦИЯ:**

### **Mobile (< 640px):**
```
┌─────────────────────────────────┐
│                                 │
│          Card 1                 │
│                                 │
└─────────────────────────────────┘
```

### **Tablet (640px - 1023px):**
```
┌────────────────┐  ┌────────────────┐
│                │  │                │
│    Card 1      │  │    Card 2      │
│                │  │                │
└────────────────┘  └────────────────┘
```

### **Desktop Small (1024px - 1279px):**
```
┌──────────┐  ┌──────────┐  ┌──────────┐
│          │  │          │  │          │
│  Card 1  │  │  Card 2  │  │  Card 3  │
│          │  │          │  │          │
└──────────┘  └──────────┘  └──────────┘
```

### **Desktop Large (≥ 1280px):**
```
┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐
│        │  │        │  │        │  │        │
│ Card 1 │  │ Card 2 │  │ Card 3 │  │ Card 4 │
│        │  │        │  │        │  │        │
└────────┘  └────────┘  └────────┘  └────────┘
```

---

## 📚 **ИСТОЧНИКИ (Context7):**

### **Embla Carousel Best Practices:**

1. **Responsive Slide Sizes:**
   ```css
   .embla__slide {
     flex: 0 0 100%;
     min-width: 0;
   }
   @media (min-width: 768px) {
     .embla__slide {
       flex: 0 0 50%;
     }
   }
   ```

2. **Gap Calculation:**
   - Gap распределяется МЕЖДУ слайдами
   - Формула: `(100% - (gap × (n - 1))) / n`

3. **Auto-detection:**
   - Embla автоматически пересчитывает при resize
   - Не нужно вручную вызывать `reInit()`

---

## ✨ **ИТОГ:**

| Критерий | Было | Стало |
|----------|------|-------|
| **Breakpoints** | 2 (768px, 1024px) | 3 (640px, 1024px, 1280px) |
| **Карточек на mobile** | 1 ✅ | 1 ✅ |
| **Карточек на tablet** | 2 ❌ (ломалось) | 2 ✅ |
| **Карточек на desktop** | 4 ❌ (ломалось) | 3-4 ✅ |
| **Математика** | Неверная ❌ | Правильная ✅ |
| **Gap** | 2rem (32px) | 1.5rem (24px) ✅ |
| **Плавность** | Резкие скачки ❌ | Плавное масштабирование ✅ |

---

**Дата:** 2025-01-25  
**Статус:** ✅ ИСПРАВЛЕНО  
**Источник:** Context7 (/davidjerleke/embla-carousel)
