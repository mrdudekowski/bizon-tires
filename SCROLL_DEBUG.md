# 🔍 ДИАГНОСТИКА ПРОБЛЕМЫ СКРОЛЛА

## ❌ Проблема: Скролл не работает

## 🛠️ ПРИМЕНЁННЫЕ ИСПРАВЛЕНИЯ

### **Изменение #1: Добавлен Flexbox контекст для `.rightPane`**

**Было:**
```css
.rightPane {
  overflow-y: auto;
  height: 100%;
  min-height: 0;
  box-sizing: border-box;
}
```

**Стало:**
```css
.rightPane {
  overflow-y: auto;
  overflow-x: hidden;
  height: 100%;
  min-height: 0;
  max-height: 100%;        /* ← Добавлено для гарантии */
  box-sizing: border-box;
  display: flex;            /* ← КРИТИЧНО: создаёт flex-контекст */
  flex-direction: column;   /* ← КРИТИЧНО: вертикальная ориентация */
}
```

**Почему это важно:**
- `display: flex` + `flex-direction: column` создаёт правильный контекст для дочерних элементов
- Теперь `.sectionTitle` и `.submenuContent` могут корректно распределяться по вертикали
- `max-height: 100%` гарантирует, что элемент не превысит высоту родителя

---

### **Изменение #2: Исправлен `.sectionTitle`**

**Было:**
```css
.sectionTitle {
  margin: 0 0 16px;
  font-size: 20px;
}
```

**Стало:**
```css
.sectionTitle {
  margin: 0 0 16px;
  font-size: 20px;
  flex-shrink: 0;  /* ← КРИТИЧНО: не сжимается */
}
```

**Почему это важно:**
- Заголовок всегда остаётся видимым
- Не участвует в расчёте overflow

---

### **Изменение #3: Исправлен `.submenuContent`**

**Было:**
```css
.submenuContent {
  display: grid;
  gap: 16px;
}
```

**Стало:**
```css
.submenuContent {
  display: grid;
  gap: 16px;
  flex: 1;              /* ← КРИТИЧНО: занимает всё доступное пространство */
  min-height: 0;        /* ← КРИТИЧНО: разрешает сжатие */
  align-content: start; /* ← Выравнивание по верху */
}
```

**Почему это важно:**
- `flex: 1` заставляет элемент занять всё оставшееся пространство в `.rightPane`
- `min-height: 0` разрешает Grid-контенту сжиматься и создавать overflow
- `align-content: start` предотвращает растягивание карточек

---

### **Изменение #4: Исправлен `.backButton`**

**Было:**
```css
.backButton {
  padding: 10px 0;
  min-width: 120px;
}
```

**Стало:**
```css
.backButton {
  padding: 10px 0;
  min-width: 120px;
  flex-shrink: 0;  /* ← Не сжимается */
}
```

---

### **Изменение #5: Мобильная версия**

**Добавлено:**
```css
@media (max-width: 768px) {
  .rightPane {
    overflow-y: auto;      /* ← Явно указано */
    overflow-x: hidden;    /* ← Добавлено */
    max-height: 100%;      /* ← Добавлено */
  }
}
```

---

## 🎯 АРХИТЕКТУРА РЕШЕНИЯ

```
.rightPane (flex column, overflow-y: auto, height: 100%)
    ├── .backButton (flex-shrink: 0) ← Не сжимается
    ├── .sectionTitle (flex-shrink: 0) ← Не сжимается
    └── .submenuContent (flex: 1, min-height: 0) ← Скроллируемая область
            └── .submenuCard × 6 ← Карточки моделей
```

### **Как это работает:**

1. `.rightPane` имеет `height: 100%` и `overflow-y: auto`
2. Внутри `.rightPane` создан flex-контекст (`display: flex; flex-direction: column`)
3. `.backButton` и `.sectionTitle` имеют `flex-shrink: 0` → всегда видны
4. `.submenuContent` имеет `flex: 1` → занимает всё оставшееся пространство
5. `.submenuContent` имеет `min-height: 0` → разрешает сжатие
6. Когда карточки в `.submenuContent` превышают доступную высоту → активируется `overflow-y: auto` на `.rightPane`

---

## 🧪 КАК ПРОВЕРИТЬ

### **Шаг 1: Откройте DevTools (F12)**

### **Шаг 2: Найдите `.rightPane` в инспекторе**

### **Шаг 3: Проверьте вычисленные стили:**

```
Computed Styles для .rightPane:
✅ display: flex
✅ flex-direction: column
✅ overflow-y: auto
✅ height: [конкретное значение в px]
✅ max-height: [то же значение]
```

### **Шаг 4: Проверьте `.submenuContent`:**

```
Computed Styles для .submenuContent:
✅ flex: 1 1 0%
✅ min-height: 0px
✅ height: [должна быть меньше суммы высот карточек]
```

### **Шаг 5: Проверьте скроллбар:**

- Если высота контента `.submenuContent` > высота `.rightPane` → должен появиться скроллбар
- Прокрутите колесиком мыши → контент должен скроллиться

---

## 🚨 ЕСЛИ ВСЁ ЕЩЁ НЕ РАБОТАЕТ

### **Возможная причина #1: Недостаточно контента**

Если в "Модели" только 6 карточек, и они все помещаются на экране → скроллбар не появится (это нормально).

**Решение:** Добавьте больше тестовых данных в `tireTypes.json`.

---

### **Возможная причина #2: Конфликтующие глобальные стили**

Проверьте, нет ли в `index.css` правил, которые переопределяют `overflow`:

```bash
# Поиск конфликтов
grep -r "overflow" src/index.css
```

---

### **Возможная причина #3: JavaScript блокирует события**

Проверьте в `BurgerMenu.jsx`, нет ли обработчиков, которые вызывают `preventDefault()` на `wheel` или `touchmove`.

---

### **Возможная причина #4: Z-index конфликт**

Убедитесь, что `.menuPanel` имеет достаточный `z-index` и не перекрывается другими элементами.

---

## 📊 КОНТРОЛЬНЫЙ СПИСОК

- [ ] `.menuBody` имеет `flex: 1`, `min-height: 0`, `overflow: visible`
- [ ] `.rightPane` имеет `display: flex`, `flex-direction: column`, `height: 100%`, `overflow-y: auto`
- [ ] `.submenuContent` имеет `flex: 1`, `min-height: 0`
- [ ] `.sectionTitle` и `.backButton` имеют `flex-shrink: 0`
- [ ] В DevTools видно, что `.rightPane` имеет ограниченную высоту
- [ ] Контент `.submenuContent` превышает высоту `.rightPane`
- [ ] Скроллбар виден в `.rightPane`
- [ ] Колесико мыши/свайп работают

---

## 🎉 ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После этих изменений скролл **ДОЛЖЕН** работать, потому что:

1. ✅ Все 4 уровня CSS исправлены
2. ✅ Flexbox контекст создан правильно
3. ✅ Grid-контент может сжиматься (`min-height: 0`)
4. ✅ Overflow не блокируется родителями
5. ✅ Box model корректен (`box-sizing: border-box`)

**Дата обновления:** 2025-01-25  
**Статус:** 🔄 ОЖИДАЕТСЯ ПОДТВЕРЖДЕНИЕ
