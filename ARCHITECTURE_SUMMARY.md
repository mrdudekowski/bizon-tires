# Архитектура лендинга БИЗОН ТИРС - Краткое описание

## ✅ Созданная структура

### 📁 Основные папки

```
bizon_tires/
├── assets/          # Все статические ресурсы
│   ├── images/      # Изображения (hero, products, advantages, testimonials, logos, icons)
│   ├── fonts/       # Шрифты
│   ├── videos/      # Видео материалы
│   └── documents/   # PDF файлы
│
├── css/             # Стили (модульная структура)
│   ├── components/  # Стили компонентов
│   ├── utilities/   # Утилиты и сброс
│   ├── responsive/  # Адаптивные стили
│   ├── variables.css # CSS переменные (Single Source of Truth)
│   └── main.css     # Главный файл (импортирует все)
│
├── js/              # JavaScript (модульная структура)
│   ├── modules/     # Функциональные модули
│   ├── utils/       # Вспомогательные функции
│   ├── config.js    # Конфигурация (Single Source of Truth)
│   └── main.js      # Точка входа
│
├── data/            # Данные (JSON)
│   ├── config.json  # Конфигурация сайта
│   └── products.json # Данные о продукции
│
├── sections/        # HTML секции (для будущей модульности)
└── docs/            # Документация
```

## 🎯 Принципы архитектуры

### 1. Single Source of Truth
- **CSS переменные**: `css/variables.css`
- **Конфигурация JS**: `js/config.js`
- **Данные сайта**: `data/config.json`
- **Данные продукции**: `data/products.json`

### 2. Модульность
- Каждый компонент в отдельном файле
- Легко добавлять новые секции
- Простое тестирование

### 3. Масштабируемость
- Готовность к добавлению сборщика
- Легко интегрировать фреймворк
- Структура поддерживает рост проекта

## 📝 Следующие шаги

1. **Обновить index.html** - подключить новую структуру CSS/JS
2. **Перенести существующие стили** - разбить `style.css` на компоненты
3. **Перенести существующий JS** - разбить `script.js` на модули
4. **Добавить данные** - заполнить `data/products.json` информацией от производителя
5. **Оптимизировать** - добавить сборщик при необходимости

## 🔄 Миграция существующего кода

Текущие файлы (`style.css`, `script.js`) можно:
- Оставить как есть (для обратной совместимости)
- Постепенно перенести в новую структуру
- Использовать как резервную копию

## 📚 Документация

- `README.md` - основная документация
- `docs/architecture.md` - подробное описание архитектуры
- `docs/deployment.md` - инструкции по деплою
- `questions_for_manufacturer.md` - вопросы для производителя

