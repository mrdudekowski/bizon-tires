# Bizon Tires Landing Page

Landing page для компании Bizon Tires, построенная на React + Vite + Tailwind CSS.

## 🚀 Технологии

- **React 19** — UI библиотека
- **Vite** — сборщик и dev-сервер
- **Tailwind CSS v4** — стилизация
- **shadcn/ui** — компоненты UI

## 📦 Установка

```bash
npm install
```

## 🛠️ Разработка

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:5173`

## 🏗️ Сборка

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 📄 Деплой на GitHub Pages

Проект настроен для автоматического деплоя через GitHub Actions.

### Настройка (один раз):

1. Перейди в настройки репозитория: **Settings** → **Pages**
2. В разделе **Source** выбери: **GitHub Actions**
3. Сохрани изменения

### Автоматический деплой:

После каждого пуша в ветку `main` workflow автоматически:
- Установит зависимости
- Соберет проект
- Задеплоит на GitHub Pages

Сайт будет доступен по адресу:
- `https://<username>.github.io/<repository-name>/` (если это project pages)
- `https://<username>.github.io/` (если репозиторий называется `username.github.io`)

### Ручной запуск деплоя:

В разделе **Actions** → выбери workflow **Deploy to GitHub Pages** → **Run workflow**

## 📁 Структура проекта

```
bizon_tires/
├── .github/
│   └── workflows/
│       └── deploy.yml      # GitHub Actions workflow
├── public/                 # Статические файлы
├── src/
│   ├── components/         # React компоненты
│   │   └── BurgerMenu/      # Бургер-меню
│   ├── App.jsx             # Главный компонент
│   ├── main.jsx            # Точка входа
│   └── index.css           # Глобальные стили
├── index.html
├── vite.config.js          # Конфигурация Vite
├── tailwind.config.js      # Конфигурация Tailwind
└── package.json
```

## 🎨 Особенности

- **Адаптивный дизайн** — работает на всех устройствах
- **Бургер-меню** — двухзонное меню с динамическим контентом
- **Плавные анимации** — переходы и эффекты
- **Оптимизация** — быстрая загрузка и производительность

## 📝 Лицензия

ISC
