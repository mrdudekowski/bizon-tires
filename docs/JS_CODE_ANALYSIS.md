# Глубокий анализ JavaScript кода

**Дата:** 2026-01-28  
**Статус:** 🔴 Критические проблемы обнаружены

---

## 🔴 Критические проблемы

### 1. Отсутствующий файл `lib/utils.js`

**Файл:** `src/components/ui/button.jsx:5`  
**Проблема:** Импорт `cn` из `@/lib/utils`, но файл не существует

```jsx
import { cn } from "@/lib/utils"  // ❌ Файл не существует
```

**Риски:**
- Приложение не запустится
- Компонент Button не работает

**Решение:**
- Создать `src/lib/utils.js` с функцией `cn`
- Или удалить неиспользуемый компонент Button

---

### 2. Неправильный путь импорта данных

**Файл:** `src/components/ProductCarousel/ProductCarousel.jsx:2`  
**Проблема:** Использование относительного пути `../../../data/tireTypes.json`

```jsx
import tireTypesData from '../../../data/tireTypes.json';  // ❌ Хрупкий путь
```

**Риски:**
- Ломается при перемещении файла
- Нарушает принцип Single Source of Truth

**Решение:**
- Использовать алиас `@/data/tireTypes.json`
- Или создать константу/конфиг для путей к данным

---

### 3. Неиспользуемая зависимость

**Файл:** `package.json:33`  
**Проблема:** `styled-components` установлен, но нигде не используется

**Риски:**
- Увеличивает размер bundle
- Путаница для разработчиков

**Решение:**
- Удалить из dependencies, если не планируется использовать

---

## ⚠️ Проблемы производительности

### 4. Отсутствие throttling/debouncing в ProductCarousel

**Файл:** `src/components/ProductCarousel/ProductCarousel.jsx:37-42`  
**Проблема:** `handleMouseMove` вызывается на каждом движении мыши без оптимизации

```jsx
const handleMouseMove = (event) => {
  // ❌ Вызывается слишком часто (может быть 100+ раз в секунду)
  if (!isDraggingRef.current) return;
  const track = trackRef.current;
  if (!track) return;
  const deltaX = event.clientX - dragStartXRef.current;
  track.scrollLeft = dragStartScrollLeftRef.current - deltaX;
};
```

**Риски:**
- Высокая нагрузка на CPU
- Проблемы с производительностью на слабых устройствах
- Потенциальные лаги при прокрутке

**Решение:**
- Использовать `requestAnimationFrame` для синхронизации с рендерингом
- Или применить throttling для ограничения частоты вызовов

---

### 5. Неоптимизированные обработчики событий

**Файл:** `src/components/ProductCarousel/ProductCarousel.jsx:49-61`  
**Проблема:** `updateArrowState` вызывается на каждом scroll и resize без оптимизации

```jsx
useEffect(() => {
  const track = trackRef.current;
  if (!track) return;

  updateArrowState();  // ❌ Синхронный вызов
  track.addEventListener('scroll', updateArrowState);  // ❌ Без throttling
  window.addEventListener('resize', updateArrowState);  // ❌ Без debouncing

  return () => {
    track.removeEventListener('scroll', updateArrowState);
    window.removeEventListener('resize', updateArrowState);
  };
}, []);
```

**Риски:**
- Множественные вызовы `setState` при прокрутке
- Лишние ре-рендеры компонента
- Проблемы с производительностью

**Решение:**
- Использовать `useCallback` для мемоизации функций
- Применить throttling для scroll (16ms = 60fps)
- Применить debouncing для resize (150-300ms)

---

### 6. Отсутствие мемоизации в BurgerMenu

**Файл:** `src/components/BurgerMenu/BurgerMenu.jsx:27-30`  
**Проблема:** `useMemo` используется, но зависимости могут быть оптимизированы

```jsx
const activeContentItem = useMemo(
  () => menuItems.find((item) => item.id === contentId),
  [contentId]  // ✅ Хорошо, но menuItems должен быть мемоизирован
);
```

**Риски:**
- Если `menuItems` пересоздается, `useMemo` не поможет
- Потенциальные лишние вычисления

**Решение:**
- Убедиться, что `menuItems` стабилен (из константы или мемоизирован)

---

## 🟡 Проблемы доступности (A11y)

### 7. Отсутствие keyboard navigation в ProductCarousel

**Файл:** `src/components/ProductCarousel/ProductCarousel.jsx`  
**Проблема:** Карусель не поддерживает навигацию с клавиатуры

**Риски:**
- Недоступна для пользователей клавиатуры
- Нарушение WCAG 2.1 (Keyboard Accessible)

**Решение:**
- Добавить обработчики `onKeyDown` для стрелок ← →
- Добавить `tabIndex` и `role="region"` с `aria-label`
- Реализовать focus management

---

### 8. Отсутствие focus trap в BurgerMenu

**Файл:** `src/components/BurgerMenu/BurgerMenu.jsx`  
**Проблема:** При открытии меню focus может уйти за пределы меню

**Риски:**
- Плохой UX для screen reader пользователей
- Нарушение WCAG 2.1 (Focus Management)

**Решение:**
- Использовать библиотеку `focus-trap-react` или реализовать свой focus trap
- Управлять focus при открытии/закрытии меню

---

### 9. Недостаточные ARIA атрибуты

**Файл:** `src/components/ProductCarousel/ProductCarousel.jsx:85-94`  
**Проблема:** Карусель не имеет полных ARIA атрибутов

```jsx
<div className="product-carousel-track"  // ❌ Нет role, aria-label, aria-live
  ref={trackRef}
  ...
>
```

**Решение:**
- Добавить `role="region"` и `aria-label="Карусель продуктов"`
- Добавить `aria-live="polite"` для объявления изменений
- Добавить `aria-atomic="true"` для контекста

---

## 🟠 Проблемы архитектуры и качества кода

### 10. Монолитный компонент App.jsx

**Файл:** `src/App.jsx`  
**Проблема:** Компонент содержит 170 строк и смешивает логику и представление

**Риски:**
- Сложно тестировать
- Сложно поддерживать
- Нарушение Single Responsibility Principle

**Решение:**
- Разбить на компоненты: `Header`, `Hero`, `ProductsSection`, `FeaturesSection`, `ContactSection`, `Footer`
- Вынести логику в кастомные хуки: `useScrollToSection`, `useMenuToggle`

---

### 11. Дублирование логики парсинга bullet points

**Файлы:**
- `src/components/ProductCarousel/ProductCarousel.jsx:64-68`
- `src/components/BurgerMenu/BurgerMenu.jsx:157`

**Проблема:** Одинаковая логика парсинга строки по символу `•` в двух местах

```jsx
// ProductCarousel.jsx
const bulletPoints = tire.description_short
  .split('•')
  .map((item) => item.trim())
  .filter(Boolean);

// BurgerMenu.jsx
description.split("•").filter(Boolean).map((item, index) => (
  <span key={index}>• {item.trim()}</span>
))
```

**Риски:**
- Нарушение DRY (Don't Repeat Yourself)
- Сложно поддерживать (изменения в двух местах)

**Решение:**
- Создать утилиту `parseBulletPoints(text)` в `src/utils/textUtils.js`
- Использовать в обоих компонентах

---

### 12. Использование index как key в map

**Файлы:**
- `src/components/ProductCarousel/ProductCarousel.jsx:74`
- `src/components/BurgerMenu/BurgerMenu.jsx:158`

**Проблема:** Использование `index` как `key` может вызвать проблемы с React

```jsx
{bulletPoints.map((item, index) => (
  <li key={index} className="product-card-item">  // ❌ Плохая практика
    • {item}
  </li>
))}
```

**Риски:**
- Проблемы с состоянием при изменении порядка элементов
- Неоптимальные ре-рендеры

**Решение:**
- Использовать уникальный ID из данных
- Или создать стабильный ключ из содержимого: `key={item.substring(0, 20)}`

---

### 13. Отсутствие обработки ошибок

**Файл:** `src/components/ProductCarousel/ProductCarousel.jsx:2`  
**Проблема:** Нет обработки случая, когда `tireTypes.json` не загрузился или пуст

**Риски:**
- Приложение упадет при отсутствии данных
- Нет graceful degradation

**Решение:**
- Добавить проверку: `if (!tireTypesData || tireTypesData.length === 0)`
- Показать fallback UI или сообщение об ошибке

---

### 14. Хардкод значений и магические строки

**Файл:** `src/App.jsx`  
**Проблема:** ID секций и классы захардкожены в JSX

```jsx
<section id="products" className="section">  // ❌ Магическая строка
  ...
  onClick={() => scrollToSection("products")}  // ❌ Дублирование
```

**Риски:**
- Опечатки приводят к багам
- Сложно рефакторить

**Решение:**
- Создать константы: `const SECTIONS = { PRODUCTS: 'products', ... }`
- Использовать везде: `id={SECTIONS.PRODUCTS}`

---

### 15. Неоптимальный useMediaQuery хук

**Файл:** `src/components/BurgerMenu/BurgerMenu.jsx:5-19`  
**Проблема:** Хук может быть улучшен для SSR безопасности

```jsx
const [matches, setMatches] = useState(() => {
  if (typeof window === "undefined") return false;  // ✅ Хорошо
  return window.matchMedia(query).matches;
});
```

**Риски:**
- Может вызвать hydration mismatch в SSR
- Не проверяет, что `window.matchMedia` существует

**Решение:**
- Добавить проверку `window.matchMedia` существования
- Использовать `useIsomorphicLayoutEffect` для SSR

---

### 16. Отсутствие PropTypes или TypeScript

**Проблема:** Нет валидации props во всех компонентах

**Риски:**
- Ошибки обнаруживаются только в runtime
- Сложно понять интерфейс компонентов

**Решение:**
- Добавить PropTypes для всех компонентов
- Или мигрировать на TypeScript

---

### 17. Отсутствие тестов

**Проблема:** Нет unit/integration тестов

**Риски:**
- Регрессии не обнаруживаются
- Сложно рефакторить безопасно

**Решение:**
- Настроить Vitest или Jest
- Написать тесты для критичных компонентов

---

## 📊 Статистика проблем

- **Критические:** 3
- **Производительность:** 3
- **Доступность:** 3
- **Архитектура:** 8
- **Всего:** 17 проблем

---

## 🎯 Приоритеты рефакторинга

### Приоритет 1 (Критично - блокирует работу)
1. Создать `src/lib/utils.js` или удалить Button компонент
2. Исправить путь импорта данных
3. Удалить неиспользуемую зависимость

### Приоритет 2 (Важно - влияет на UX)
4. Оптимизировать производительность ProductCarousel
5. Добавить keyboard navigation
6. Добавить focus trap в BurgerMenu

### Приоритет 3 (Улучшение - технический долг)
7. Разбить App.jsx на компоненты
8. Вынести дублирующуюся логику
9. Добавить обработку ошибок
10. Добавить константы вместо магических строк

---

## 📝 Рекомендации

1. **Немедленно:** Исправить критические проблемы (1-3)
2. **В ближайшее время:** Оптимизировать производительность (4-5)
3. **Постепенно:** Улучшить архитектуру и доступность (6-17)
