# Инструкция по настройке и использованию новой архитектуры

## 🔄 Миграция существующего кода

### Вариант 1: Постепенная миграция (рекомендуется)

1. **Оставьте старые файлы** (`style.css`, `script.js`) для обратной совместимости
2. **Подключите новую структуру** в `index.html`:
   ```html
   <!-- CSS -->
   <link rel="stylesheet" href="css/main.css">
   
   <!-- JavaScript (в правильном порядке) -->
   <script src="js/config.js"></script>
   <script src="js/utils/validation.js"></script>
   <script src="js/utils/api.js"></script>
   <script src="js/modules/navigation.js"></script>
   <script src="js/modules/form.js"></script>
   <script src="js/modules/animations.js"></script>
   <script src="js/main.js"></script>
   ```
3. **Постепенно переносите функциональность** из старых файлов в новые модули
4. **Удалите старые файлы** после полной миграции

### Вариант 2: Полная миграция сразу

1. Обновите `index.html` для использования новой структуры
2. Перенесите весь код из `style.css` в соответствующие компоненты
3. Перенесите весь код из `script.js` в соответствующие модули
4. Удалите старые файлы

## 📝 Порядок подключения скриптов

Важно соблюдать порядок загрузки:

1. **config.js** - конфигурация должна быть загружена первой
2. **utils/** - утилиты загружаются перед модулями
3. **modules/** - модули используют утилиты и конфигурацию
4. **main.js** - точка входа загружается последней

## 🎨 Использование CSS переменных

Все цвета, отступы и другие значения определены в `css/variables.css`:

```css
/* Использование в компонентах */
.my-component {
    color: var(--color-white);
    padding: var(--spacing-md);
    border-radius: var(--border-radius);
    transition: var(--transition);
}
```

## ⚙️ Использование конфигурации

Конфигурация доступна глобально через `window.CONFIG`:

```javascript
// В любом модуле
const phone = CONFIG.contacts.phone;
const apiUrl = CONFIG.api.endpoints.submitForm;
```

## 📦 Добавление новых компонентов

### Новый CSS компонент:

1. Создайте файл `css/components/new-component.css`
2. Добавьте импорт в `css/main.css`:
   ```css
   @import url('./components/new-component.css');
   ```

### Новый JS модуль:

1. Создайте файл `js/modules/new-module.js`
2. Подключите в `index.html` после других модулей
3. Вызовите инициализацию в `js/main.js` (если нужно)

## 🔍 Поиск нужного файла

- **Стили компонента** → `css/components/`
- **JavaScript функциональность** → `js/modules/`
- **Вспомогательные функции** → `js/utils/`
- **Данные** → `data/`
- **Изображения** → `assets/images/`

## ✅ Чек-лист после создания структуры

- [x] Создана структура папок
- [x] Созданы базовые файлы
- [x] Настроены CSS переменные
- [x] Настроена конфигурация JS
- [ ] Обновлен index.html для использования новой структуры
- [ ] Перенесен код из старых файлов
- [ ] Протестирована работа сайта
- [ ] Удалены старые файлы (опционально)

