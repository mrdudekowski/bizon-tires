# Анализ и рефакторинг src/index.css

**Дата:** 2026-01-28  
**Цель:** Оптимизация для поддержки и масштабируемости

---

## 🔍 Выявленные проблемы

### 1. Разрозненные медиа-запросы

**Проблема:** 8 отдельных `@media` запросов разбросаны по файлу:
- `@media (min-width: 768px)` - строка 158
- `@media (min-width: 640px)` - строка 471
- `@media (min-width: 1024px)` - строки 482, 596
- `@media (min-width: 1280px)` - строка 493
- `@media (max-width: 639px)` - строка 580
- `@media (max-width: 1024px)` - строка 606
- `@media (prefers-reduced-motion: reduce)` - строка 612

**Риски:**
- Сложно отслеживать breakpoints
- Дублирование логики
- Несоответствие с Tailwind breakpoints (sm: 640px, md: 768px, lg: 1024px, xl: 1280px)

---

### 2. Комментарии, объясняющие "что" вместо "почему"

**Проблемные комментарии:**

```css
/* ❌ Объясняет "что" */
height: calc(var(--header-height) - 8px); /* ещё крупнее, но с небольшим отступом */
padding: 0 44px; /* Space for arrows */
grid-auto-columns: 100%; /* Mobile: 1 card full width */
scrollbar-width: none; /* Firefox */
-ms-overflow-style: none; /* IE/Edge */
-webkit-overflow-scrolling: touch; /* iOS momentum scrolling */
display: none; /* Chrome/Safari/Opera */
min-height: 180px; /* Ensure consistent height */
/* Tablet: 2 cards */
/* Desktop Small: 3 cards */
/* Desktop Large: 4 cards */
/* Navigation Arrows */
/* На планшетах и десктопах логотип смещаем ближе к бургеру */
```

**Хорошие комментарии (объясняют "почему"):**
```css
/* ✅ Объясняет "почему" */
/* Base card styles - Single Source of Truth */
/* Product Carousel - Native Scroll Snap Implementation */
/* Product Card - extends .card-base for consistency */
```

---

### 3. Дублирующиеся стилевые паттерны

#### Паттерн 1: Базовые стили кнопок
```css
/* Дублируется в .cta-button и .ghost-button */
display: inline-flex;
align-items: center;
justify-content: center;
padding: 10px 18px;
border-radius: var(--radius);
outline: none;
```

#### Паттерн 2: Заголовки карточек
```css
/* Дублируется в .info-card-title и .product-card-title */
margin: 0;
font-size: 18px;
font-weight: 700;
color: var(--color-foreground);
line-height: 1.3;
```

#### Паттерн 3: Контейнеры с max-width
```css
/* Дублируется в .navbar, .section, .footer-content */
max-width: var(--container-max);
margin: 0 auto;
```

#### Паттерн 4: Focus-visible стили
```css
/* Дублируется для всех кнопок */
outline: 2px solid var(--color-ring);
outline-offset: 2px;
```

---

## ✅ Предлагаемые изменения

### 1. Объединение медиа-запросов

**Подход:** Использовать Tailwind breakpoints через `@screen` или объединить в группы

```css
/* ✅ Группировка по breakpoints */
@screen sm {
  /* 640px+ */
}

@screen md {
  /* 768px+ */
}

@screen lg {
  /* 1024px+ */
}

@screen xl {
  /* 1280px+ */
}
```

**Альтернатива:** Объединить все медиа-запросы для одного компонента в один блок

---

### 2. Улучшение комментариев

**Принцип:** Комментарии должны объяснять "почему", а не "что"

```css
/* ❌ Было */
height: calc(var(--header-height) - 8px); /* ещё крупнее, но с небольшим отступом */

/* ✅ Стало */
/* Уменьшаем высоту логотипа на мобильных для визуального баланса с burger-кнопкой */
height: calc(var(--header-height) - 8px);
```

```css
/* ❌ Было */
padding: 0 44px; /* Space for arrows */

/* ✅ Стало */
/* Отступы для размещения навигационных стрелок без перекрытия контента */
padding: 0 44px;
```

---

### 3. Вынос общих паттернов

#### Создать базовый класс для кнопок:
```css
.button-base {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 10px 18px;
  border-radius: var(--radius);
  outline: none;
  transition: transform var(--transition-fast);
}

.cta-button {
  @extend .button-base;
  background: var(--color-primary);
  color: var(--color-on-primary);
}

.ghost-button {
  @extend .button-base;
  background: transparent;
  border: 1px solid var(--color-foreground);
  color: var(--color-foreground);
}
```

#### Создать базовый класс для заголовков карточек:
```css
.card-title-base {
  margin: 0;
  font-size: 18px;
  font-weight: 700;
  color: var(--color-foreground);
  line-height: 1.3;
}

.info-card-title,
.product-card-title {
  @extend .card-title-base;
}
```

#### Создать утилитарный класс для контейнеров:
```css
.container-base {
  max-width: var(--container-max);
  margin: 0 auto;
}
```

#### Создать CSS-переменную для focus-visible:
```css
:root {
  --focus-outline: 2px solid var(--color-ring);
  --focus-offset: 2px;
}
```

---

## 📝 Список изменений для index.css

### Приоритет 1 (Критично):

1. **Объединить медиа-запросы для карусели** (строки 471-501)
   - Объединить все `@media` для `.product-carousel` в один блок
   - Использовать `@screen` из Tailwind для консистентности

2. **Объединить медиа-запросы для навигации карусели** (строки 580-604)
   - Объединить все `@media` для `.carousel-nav` в один блок

3. **Улучшить комментарии**
   - Удалить комментарии, объясняющие "что"
   - Добавить комментарии, объясняющие "почему" там, где это необходимо

### Приоритет 2 (Важно):

4. **Создать базовые классы для кнопок**
   - `.button-base` с общими стилями
   - Использовать для `.cta-button` и `.ghost-button`

5. **Создать базовый класс для заголовков карточек**
   - `.card-title-base` с общими стилями
   - Использовать для `.info-card-title` и `.product-card-title`

6. **Создать утилитарный класс для контейнеров**
   - `.container-base` с `max-width` и `margin: 0 auto`
   - Использовать для `.navbar`, `.section`, `.footer-content`

### Приоритет 3 (Улучшение):

7. **Вынести focus-visible стили в CSS-переменные**
   - Создать `--focus-outline` и `--focus-offset`
   - Использовать во всех focus-visible правилах

8. **Добавить CSS-переменные для часто используемых значений**
   - `--spacing-button-padding: 10px 18px`
   - `--spacing-card-padding: 20px`
   - `--spacing-card-gap: 8px` / `--spacing-card-gap-large: 12px`

---

## 🎯 Гайдлайн по стилизации для команды

### Как писать стили для новых компонентов

**Принцип:** Tailwind-first подход. Используйте Tailwind классы для всех стилей, кроме сложных анимаций. Если нужны глобальные стили — расширяйте `.card-base` или создавайте новые базовые классы по аналогии. Всегда проверяйте, нет ли уже существующего паттерна в дизайн-системе.

### Когда допустимо использовать CSS Modules

**Правило:** CSS Modules только для сложных анимаций с множественными keyframes или синхронизированными переходами между несколькими элементами. Простые hover/focus эффекты, transitions и transforms должны быть в Tailwind классах. Пример оправданного использования: `BurgerToggle` с комплексной трансформацией иконки.

### Как расширять дизайн-систему

**Процесс:** 1) Проверь, есть ли нужный паттерн в `.card-base`, `.button-base` или других базовых классах. 2) Если нет — создай новый базовый класс в `index.css` с префиксом `-base` и добавь CSS-переменные в `:root` для значений. 3) Используй этот базовый класс в компонентах, расширяя его специфичными стилями. 4) Документируй новый паттерн в `docs/`.

---

## 📊 Рекомендации по поддержке масштабируемости

### 1. Структура CSS файла

```
1. Tailwind директивы (@tailwind, @config, @plugin)
2. CSS переменные (:root)
3. Глобальные reset стили (*, html, body, a)
4. Утилитарные классы (.skip-link, .page--blurred)
5. Базовые классы (.card-base, .button-base, .container-base)
6. Компонентные классы (расширяющие базовые)
7. Медиа-запросы (сгруппированные по компонентам)
8. @layer base (Tailwind base стили)
```

### 2. Правила именования

- **Базовые классы:** `*-base` (например, `.card-base`, `.button-base`)
- **Компонентные классы:** `component-element` (например, `.info-card`, `.product-card`)
- **Модификаторы:** `component-element--modifier` (например, `.carousel-nav--prev`)

### 3. Принципы расширения

- **DRY (Don't Repeat Yourself):** Если стили повторяются 2+ раза — выноси в базовый класс
- **Single Source of Truth:** Все значения через CSS-переменные
- **Mobile-first:** Базовые стили для мобильных, медиа-запросы для расширения
- **Композиция:** Используй базовые классы + специфичные стили вместо дублирования

### 4. Чеклист для новых стилей

- [ ] Проверен существующий базовый класс?
- [ ] Созданы CSS-переменные для значений?
- [ ] Комментарии объясняют "почему", а не "что"?
- [ ] Медиа-запросы сгруппированы по компонентам?
- [ ] Использованы Tailwind breakpoints (`@screen`)?
- [ ] Стили следуют mobile-first подходу?

---

**Статус:** ✅ Анализ завершен. Готов к рефакторингу.
